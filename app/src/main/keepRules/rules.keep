# Shizuku creates this UserService reflectively in its privileged process. The stable UserService
# tag permits class-name obfuscation; keep only the class and supported constructor signatures.
-keep,allowobfuscation class net.extrawdw.apps.unstop.UnstopUserService {
    public <init>();
    public <init>(android.content.Context);
}
