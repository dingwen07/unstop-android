# Shizuku creates this UserService reflectively in its privileged process. Keep only the class and
# supported constructors; its Binder methods remain available through the generated AIDL stub.
-keep class net.extrawdw.apps.unstop.UnstopUserService {
    public <init>();
    public <init>(android.content.Context);
}
